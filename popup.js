// function getCurrentTabUrl(callback) {
 
//   var queryInfo = {
//     active: true,
//     currentWindow: true
//   };

//   chrome.tabs.query(queryInfo, (tabs) => {
//     var tab = tabs[0];
//     var url = tab.url;
// 	str =tab.url;

//     console.assert(typeof url == 'string', 'tab.url should be a string');

//     callback(url);
//   });
// }



document.addEventListener('DOMContentLoaded', () => {
   /*if(dropdown.value == 'currTab')
   {
	   opt = 'currTab';
   }
   else
   {
	   opt = 'newTab';
   }*/
});
	  